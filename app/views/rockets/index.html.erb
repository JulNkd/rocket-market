<div class="container">
  <h1>COUCOU</h1>
  <ul>
    <% @rockets.each do |rocket| %>
      <li><%= link_to rocket.name, rocket_path(rocket) %></li>
    <% end %>
  </ul>
  <%= link_to "Add a new rocket", new_rocket_path %>
</div>
